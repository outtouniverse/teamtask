<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Login</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css"
      integrity="sha512-DTOQO9RWCH3ppGqcWaEA1BIZOC6xxalwEsw9c2QQeAIftl+Vegovlnee1c9QX4TctnWMn13TZye+giMm8e2LwA=="
      crossorigin="anonymous"
      referrerpolicy="no-referrer"
    />
  </head>
  <body
    class="bg-gray-900 text-gray-300 flex items-center justify-center min-h-screen"
  >
    <div
      class="w-full max-w-md p-8 bg-gray-800 border-gray-100/5 border-2 transform transition-all"
    >
      <h2
        class="text-4xl font-extrabold text-center text-white mb-8 tracking-tight"
      >
        Welcome Back
      </h2>

      <% if (typeof error !== 'undefined' && error) { %>
      <p
        class="text-red-400 bg-red-900 text-center py-2 border border-red-700 mb-6"
      >
        <%= error %>
      </p>
      <% } %>

      <form action="/users/login" method="post" class="space-y-6">
        <div>
          <label
            for="username"
            class="block text-sm font-medium text-gray-300 mb-2"
            >Username</label
          >
          <input
            type="text"
            id="username"
            name="username"
            class="w-full px-6 py-4 bg-gray-800 border-2 border-gray-700 text-white focus:outline-none focus:ring-2 focus:ring-indigo-600 placeholder-gray-500"
            placeholder="Enter your username"
            required
          />
        </div>
        <div>
          <label
            for="password"
            class="block text-sm font-medium text-gray-300 mb-2"
            >Password</label
          >
          <input
            type="password"
            id="password"
            name="password"
            class="w-full px-6 py-4 bg-gray-800 border-2 border-gray-700 text-white focus:outline-none focus:ring-2 focus:ring-indigo-600 placeholder-gray-500"
            placeholder="Enter your password"
            required
          />
        </div>
        <button
          type="submit"
          class="w-full bg-indigo-700 text-white py-4 border-2 border-indigo-600 focus:outline-none"
        >
          Login
        </button>
      </form>

      <p class="text-sm text-center mt-6 text-gray-400">
        Don't have an account?
        <a href="/users/signup" class="text-indigo-400 underline">Sign up</a>
      </p>
    </div>
  </body>
</html>
